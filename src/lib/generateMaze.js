const generateMaze = function generateMaze(ctx, rows, cols) {
  for (let i = 0; i < rows; i++) {
    for (let j = 0; j < cols; j++) {
      console.log(i + j)
    }
  }
};

export default generateMaze;
